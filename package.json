{
  "name": "auto-elec",
  "version": "1.0.2",
  "main": "main.js",
  "license": "MIT",
  "devDependencies": {
    "electron": "^8.5.1",
    "electron-builder": "^22.3.2"
  },
  "dependencies": {
    "electron-updater": "^4.2.2"
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kaloudiyi/auto-elec.git"
  }
}
